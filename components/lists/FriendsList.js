// import Card from '../cards/';

export default function FriendsList({ friends }) {

  return (
    <ScrollView>
      <View>
        <Text>Hello from FriendsList!</Text>
      </View>
    </ScrollView>
  )
}